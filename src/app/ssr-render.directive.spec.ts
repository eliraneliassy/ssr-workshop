import { SsrRenderDirective } from './ssr-render.directive';

describe('SsrRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new SsrRenderDirective();
    expect(directive).toBeTruthy();
  });
});
