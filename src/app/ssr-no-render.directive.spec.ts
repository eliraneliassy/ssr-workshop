import { SsrNoRenderDirective } from './ssr-no-render.directive';

describe('SsrNoRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new SsrNoRenderDirective();
    expect(directive).toBeTruthy();
  });
});
